<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="au.edu.uow.fyp01.abas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".Activity.ClassListActivity" />
        <activity android:name=".Activity.StudentListActivity" />
        <activity android:name=".Activity.RecordActivity" />
        <activity android:name=".Activity.RecordOverviewActivity" />
        <activity android:name=".Activity.CommentListActivity" />
        <activity android:name=".Activity.FileSharingHome" />
        <activity android:name=".Activity.FileSharingAdd" />
        <activity android:name=".Activity.FileReceiveHome"></activity>
    </application>

</manifest>